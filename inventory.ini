# Inventory for the OpenObserve production stack.
#
# Deploying to the machine you run Ansible ON (local):
[local]
localhost ansible_connection=local

# Deploying to a REMOTE clean host (replace vars/hostname, ensure SSH key auth):
#[vms]
#o2-host ansible_host=192.168.122.151 ansible_user=xzizka ansible_become=true

# A playground host group used when demo app targets host from containers.
[o2]
localhost ansible_connection=local

[all:vars]
ansible_python_interpreter=/usr/bin/python3
